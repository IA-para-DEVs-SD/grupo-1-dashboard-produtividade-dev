# KiroRails Hook Configuration
# Edit these commands to match your project's build tools.

# Compilation / type check (backend)
compile=cd backend && uv run python -m py_compile src/main.py

# Test suite
test=cd backend && uv run pytest -q

# Linter / static analysis
lint=cd backend && uv run ruff check .

# Security scan (optional)
security=
